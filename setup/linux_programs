#!/bin/bash

# Programs to be installed in linux system
echo "Do you want to install programs specified in Brewfile? (y/n)"
read

if [[ $REPLY = [yY] ]]; then 
        echo "No programs set!"
    else 
        echo "Skipping programs" 
    fi